<% if line_item == @current_item %> 
  <tr id="current_item">
<% else %>
  <tr>
<% end %>
    <td><%= line_item.quantity %></td>
    <td><%= line_item.product.name %></td>
    <td class="right"><%= number_to_currency(line_item.total_price) %></td>
  </tr>
